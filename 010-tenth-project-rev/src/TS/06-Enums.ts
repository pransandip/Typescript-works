const enum Choice {
  FIRST,
  SECOND,
  THIRD,
}

const sr_choice = Choice.FIRST;
console.log(sr_choice);

/*---------BREAK-----------*/
const enum SeatChoice {
  AISLE = "aisle",
  MIDDLE = 10,
  WINDOW,
}

const srSeat = SeatChoice.WINDOW;
console.log(srSeat);
